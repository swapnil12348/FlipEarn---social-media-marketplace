"use strict";require('dotenv').config(); // Ensure your .env file is loaded
const { defineConfig } = require('@prisma/config');

module.exports = defineConfig({
  // Explicitly telling Prisma where the schema file is as a STRING
  schema: {
    kind: 'single',
    filePath: './prisma/schema.prisma'
  },
  migrate: {
    // Falls back to DATABASE_URL if DIRECT_URL doesn't exist
    url: process.env.DIRECT_URL || process.env.DATABASE_URL
  }
}); /* v9-3f43b36b4381e330 */
